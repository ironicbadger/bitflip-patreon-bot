services:
  patreon-discord-bot:
    build: .
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    volumes:
      - ./data:/data
    ports:
      - "127.0.0.1:${WEBHOOK_PORT:-3000}:${WEBHOOK_PORT:-3000}"
